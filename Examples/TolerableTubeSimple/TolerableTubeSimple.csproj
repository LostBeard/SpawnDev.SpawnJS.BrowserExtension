<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">

	<PropertyGroup>
		<TargetFramework>net10.0</TargetFramework>
		<Nullable>enable</Nullable>
		<ImplicitUsings>enable</ImplicitUsings>
		<!-- Disable compression (optional, based on use case) -->
		<CompressionEnabled>false</CompressionEnabled>
	</PropertyGroup>

	<ItemGroup>
		<!-- Direct reference so this WASM app gets the SpawnJS.WebWorkers build targets that produce
		     main.classic.js (the background service worker loads it via importScripts). -->
		<PackageReference Include="SpawnDev.SpawnJS.WebWorkers" Version="2.0.3" />
		<PackageReference Include="SpawnDev.SpawnJS.BrowserExtension" Version="2.0.0" />
		<PackageReference Include="SpawnDev.SpawnJS.RazorUI" Version="2.0.2" />
	</ItemGroup>

</Project>
